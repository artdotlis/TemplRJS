class AttrHtml {
    static readonly replRef: string = 'repl';

    static readonly cslId: string = 'data-csl-entry-id';

    static readonly slideVisUnc: [string, string] = ['data-visibility', 'uncounted'];
}

export default AttrHtml;
